apiVersion: v1
kind: Service
metadata:
  name: kodezera-redis
  namespace: kodezera
  labels:
    app: kodezera
    component: redis
spec:
  selector:
    app: kodezera
    component: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
